# 草料二维码安全设备看板 — 开发复盘报告

> 日期：2026-07-28
> 参与角色：用户（需求方 / 数据 owner）、WorkBuddy（开发实施）
> 项目产物：`cli-dashboard`（Express + Chart.js 实时看板，部署于 Railway）

---

## 一、项目背景与目标

用户使用 **草料二维码** 平台管理工厂安全设备（灭火器、急救药箱、应急灯、洗眼器、压力表、消防气瓶等），数据分散在平台各码、各表单中，无法集中分析。目标：

1. 把草料平台数据汇聚成**实时**分析看板；
2. 团队可随时通过浏览器访问；
3. 覆盖设备状态、效期预警、计划执行等多维度分析。

最终交付：一个直连草料官方数据库、实时反映最新数据的 Web 看板，部署在云端（Railway），团队任意设备浏览器访问。

---

## 二、关键里程碑（时间线）

| 阶段 | 时间 | 内容 | 关键决策 |
|------|------|------|----------|
| 1. 首次同步 | 7/27 | 直连草料官方 MySQL 只读库，导出 35 张表 / 31462 条记录；本地生成 HTML 报告 | Python pymysql 安装失败 → 改用 **Node mysql2** |
| 2. 实时化 | 7/27 | 用户要求"实时看报表" → 选定**云端部署** | 建 Express + Chart.js 看板，推 GitHub，部署 Railway |
| 3. 五大功能 | 7/27 | 日期切片、急救药箱效期、灭火器维修充装预警、周期任务到期、533 灭火器多维分析 | 后端补 5 个 API，前端加交互 |
| 4. 稳定性修复（第一波） | 7/27 | 解决"一直加载中"、"dbStatus 永久转圈" | `Promise.allSettled` + 超时 + no-cache + dbStatus 徽章；`node --check` 修 JS 语法错误 |
| 5. 数据一致性 | 7/27 | 急救药箱碘伏显示 2025-10-04（过期），草料前台是 2027-03-01 | 误读 `table_d24` 表单日志 → 改读 `template_codeinfo_d25` 主数据表 |
| 6. 二次五功能 + 收尾 | 7/28 | 饼图数据标签、超期仅最新周期、按生产厂家分析、明细精确筛选、强制报废预警 | GitHub git push 受沙箱网络拦截 → 改用 **GitHub REST Contents API** 推送成功，Railway 重部署验证 |
| 7. P0 加固（稳+安） | 7/28 | ① 查询缓存层（统计接口默认缓存 60s，降 RDS 压力）② 令牌鉴权（`?token=`/请求头，数据不再裸奔）③ 字段缺失降级容错（safeSelect，草料改字段不致整接口 500）④ 前端组件化（index.html 拆分为 core/sections/app 三模块） | server.js 加缓存+鉴权中间件+safeSelect；前端拆外链 JS；本地 3200 全量冒烟 + Railway 线上复测通过 |
| 8. 登录弹窗 + P1 自动冒烟 | 7/28 | ① 原部署地址打开后弹出**登录对话框**，输入令牌才进入（令牌存 sessionStorage，不再明文暴露在 URL）② P1：新增 `scripts/smoke-test.js` 自动冒烟 + GitHub Actions（推送即跑语法+鉴权闸门） | server.js 加 `/api/auth/verify`；前端加 modal + `initAuth()` 流程；`node --check` 4 文件 + 鉴权 200/401 + 可选 DB 接口冒烟全绿 |
| 9. 柱状图数值 + 应急灯多维分析 | 7/28 | ① **所有柱状图顶部显示数值**（离线自研 `barValues` 插件，不依赖 CDN）② **消防应急灯多维度分析**：按设备/按检查人/按检查结果三维度柱状图 + 设备检查明细表（检查次数/异常/最近检查时间）+ 异常记录明细表（结果非"正常"的 4 条） | 新增 `server.js /api/emergency-lights/analysis`（聚合 byDevice/byInspector/byResult/abnormal，支持日期筛选）；前端加 `loadLightAnalysis`/`renderLightAnalysis`/`renderLightDetail`/`renderLightAbnormal` 与对应 section；本地 3203 全量接口验证通过 |
| 10. 时间戳 8 小时偏差修复 | 7/28 | 用户反馈"李伟东今天 11:26 提交，看板显示 19:26"。根因：草料库 DATETIME 以**北京时间**存储，但 Railway 服务器时区是 **UTC**，mysql2 把 `11:26` 当 UTC → 序列化 `...T11:26Z` → 浏览器按中国时区 +8 显示成 19:26。修复：① `server.js` 顶部固定 `process.env.TZ='Asia/Shanghai'`，mysql2 按北京时间解析 → 序列化 `...T03:26Z` → 浏览器显示 11:26；② 前端日期选择器改用本地年月日拼串（避免 `toISOString` 凌晨差一天）。作用范围覆盖**所有带时间的接口**（点检记录、任务周期、到期时间、`/api/records/latest`）。数据模型再次确认：`table_dXX`=表单提交历史、`template_codeinfo_*`=子码主数据（当前字段值/效期）、`code_task_log`=计划执行情况、`base_table_data`=全表单汇总视图。 | 本地 `scripts/check-tz.js` 实查李伟东记录：修复后 `2026-07-28T03:26:54Z`→北京 11:26；线上 `/api/records/latest` 复测李伟东两条 11:26 记录 JSON 为 `...T03:26:54Z`，浏览器显示 11:26:54，偏差消除 |
| 11. 时间切片落实 + 按设备类型总览 | 7/28 | 用户提议"时间切片应针对每个分析视图、不是全局一刀切；并按设备类型分析基础码+检查记录+任务周期"。落地：① **时间切片落到每个分析视图**：顶部全局日期筛选现已对 `stats`(记录数/时间范围)、`tasks/overdue`、`records/latest`、`fire-extinguisher/results`、`emergency-lights/results`、告警区(`loadAlerts`) 等全部生效，各视图按其自身数据源时间列切片（点检类用 `记录时间`、计划/任务类用 `截止时间`/`开始时间`），消除"选了时间段却分析全库"。② **新增按设备类型总览**：`/api/device-type/overview` 以 `base_codeinfo.目录` 为基准，跨 `base_table_data`(检查记录)+`code_task_log`(任务周期) 按设备类型（灭火器/应急灯/洗眼器/急救药箱/压力表/气瓶/喷淋泵/应急物资）聚合 基础码数/检查次数/最近检查时间/任务完成率；前端新增 section+柱状图。 | 修 bug：device-type 聚合时 `SUM()` 由 mysql2 返回字符串，致 `e.taskCompleted += r.completed` 变**字符串拼接**（急救药箱完成率 46971.5%），改 `Number()` 强转后正常（95.4%）。本地 3206 + 线上复测：stats 全量 8853→切片(7/1~7/28)752；设备类型完成率均合理（灭火器99.0%/应急灯97.9%/急救95.4%/洗眼94.4%/喷淋97.6%/应急物资88.9%）；`npm run smoke` 全绿 |
| 12. 应急灯维度改责任部门/厂商 + endDate 边界修复 | 7/28 | 用户要求：① **应急灯多维度分析取消"按设备/按检查结果"，增加"按责任部门/按厂商"**；② 反馈"最下方最新提交记录看不到今天李伟东/缪庆华"。落地：应急灯接口 `table_d16` JOIN 主数据表 `template_codeinfo_d15`（按 `code_id` 关联，含 `责任部门_81036793872385`/`生产厂家_3170558`），新增 `byDepartment`(8 部门)/`byVendor`(厂商，SQL 内 `TRIM(REPLACE(...,UNHEX('E38080'),' '))` 归一化全角空格脏数据)，删 `byResult`；前端 tab 改为 按检查人/按责任部门/按厂商，默认 按检查人。 | ① 修边界 bug：`endDate` 原用 `记录时间 <= 'YYYY-MM-DD'`（即当天 00:00）把当天记录整段排除→统一用 `endVal()` 补成 `YYYY-MM-DD 23:59:59`，选"今天"能含当天 11:26/13:07 等；线上复测 `/api/records/latest` 李伟东/缪庆华今日记录已显示。② 全角空格 U+3000 归一化：MySQL `CHAR(12288)` 在本库不返回 U+3000（返回 codepoint 48），改用 `UNHEX('E38080')` 字节匹配才生效，"易发"220+"易发　"58 合并为 278；`npm run smoke` 全绿 |
| 13. 应急灯分析/明细改读 d15 主数据（去 d16、去异常明细） | 7/28 | 用户要求：**应急灯多维度分析应从 `template_codeinfo_d15`（仅 83 台设备）取数，不再从 `table_d16` 点检记录做多维分析；设备检查明细查询也只从 d15 取，且不体现"点检异常明细"**。落地：① `/api/emergency-lights/analysis` 重写为**纯 d15 设备台账聚合**——维度 按检查人(`点检人_3170560`)/按责任部门(`责任部门_81036793872385`)/按厂商(`生产厂家_3170558`)，三者全部 `TRIM(REPLACE(...,UNHEX('E38080'),' '))` 归一化；`total`=83；新增 `devices` 台账数组（编码/安装位置/责任部门/厂商/点检人/状态/规格型号/出厂日期）。② 前端：分析标题"共 X 个设备"；明细表改为 d15 台账（6 列），搜索覆盖编码/位置/部门/厂商/点检人；**删除"异常记录明细"卡片与 `renderLightAbnormal`**，去掉"仅看有异常"筛选；`loadLightAnalysis` 不再调用异常渲染。③ d15 无"点检时间"字段，分析为全量台账快照，时间切片对其不适用（不再套用 `endVal()`）。 | 全量冒烟：线上 `/api/emergency-lights/analysis` `total=83`、`devices` 83 条、三维度数与台账字段均正确；"易发"25/"艺光"20（全角空格已合并）；`npm run smoke` 全绿。保留项（待用户确认是否也要去掉）：`/api/emergency-lights/trend`（检查趋势，仍来自 d16 点检量）、`/api/emergency-lights/results` 的"应急灯-异常"段（检查结果 doughnut）。 |
| 14. 精简看板 + 明细筛选强化 + 全局导出Excel | 7/28 | 用户要求：① **删除三个分析区块**——"按设备类型总览"、"计划任务到期与超期分析"、"设备状态监控"（压力表/气瓶/检查结果三图）；② 应急灯右侧表格标题改为**"消防应急灯明细表"**，新增**按责任部门/按点检人**下拉筛选；③ 灭火器明细表新增**按点检人/按规格**筛选，**删除按生产厂家**筛选；④ **全局所有表格加"导出Excel"按钮**。落地：前端删 3 个 section 及 `loadPressure`/`loadGas`/`loadResults`/`loadTaskDeadline`/`loadDeviceTypeOverview` 五个 loader（`loadAllData` 任务数组同步精简，后端对应端点保留为死代码暂不动）；应急灯 filter-bar 加 `lightDeptFilter`/`lightInspectorFilter`（从 d15 devices 去重填充）；灭火器 filter-bar 改 `fireInspectorFilter`/`fireSpecFilter`、去 `fireMfFilter`；`core.js` 新增 `exportTable(tableId, fileName)`——**纯前端零依赖**：克隆 table→剔除按钮→包 UTF-8 BOM 的 HTML 存为 `.xls` Blob 下载；9 个表格（药品效期/维修充装/强制报废/灭火器明细/应急灯明细/计划执行明细/最新提交/两个排行榜）各加 `📥 导出Excel`。 | `node --check` core/sections/app/server 全过；Grep 校验活跃源码无 `deviceType`/`taskDeadline`/`pressureChart`/`fireMfFilter` 等悬挂引用（仅 index.html.bak 备份残留，不影响）；GitHub Contents API 推送 + Railway 重部署后线上复测通过。 |
| 15. 人员排行改枢纽分析 + 删灭火器TOP15 | 7/28 | 用户要求：① 人员参与度排行**导出 Excel 为全部人员**，页面**展示 TOP 20 即可**；② 展示每人提交的**是哪张表、各多少次**（类似枢纽/透视表）；③ **删除"灭火器检查提交 TOP 15"表格**。落地：`/api/members/ranking` 重写为**枢纽聚合**——SQL 按 `记录人 × 记录单名称` 分组（不再 LIMIT 20），Node 端组装 `{forms(按表总量降序), formTotals, members[{name,total,byForm}], grandTotal}`；前端人员排行改**单卡全宽动态列表格**：表头 `# / 姓名 / 总提交数 / 每张记录单一列(含该表合计)`，行内每格显示该人该表次数（0 显示"-"），仅渲染 TOP 20，标题旁显示"展示 TOP 20 / 共 N 人，合计 X 条"；导出按钮改调 `exportMemberRanking()`——从内存全量数据拼 HTML 表（含合计行）导出**全部人员**，文件名"人员参与度排行-全部人员按表单枢纽分析.xls"；`core.js` 抽出 `downloadExcelHtml()` 复用 Blob 下载逻辑；删除灭火器 TOP15 卡片 + `loadFireRanking`（后端 `/api/fire-extinguisher/ranking` 留作死代码）。 | 本地实测：12 张记录单 × 72 人、grandTotal=8853、逐人合计=grandTotal 校验通过；王成良 1284（灭火器 681+应急灯 443+…）；`node --check` 4 文件全过；推送 + Railway 复测通过。 |
| 16. 发奖口径确认 + 记录人归一化 | 7/28 | 用户目标：**定期按每人"在草料系统的全部提交记录总次数"发放小礼品奖励**，要求先调研统计口径。调研结论：`base_table_data` 实为**全量提交历史 UNION**（8853 行 = 12 张 `table_dXX` 之和），并非"每码最新一条"视图——故当前看板"总提交数"已是**真实累计提交次数**，发奖口径准确、无需换数据源。用户拍板：① **全部计入**（含"码内容修改"713、"状态更新记录"187 两类管理类提交）；② **记录人做归一化**。落地：`server.js` 新增全局 `norm(col)=TRIM(REPLACE(col,UNHEX('E38080'),' '))`；`/api/members/ranking` 的 `记录人` 经 `norm()` 取别名 `name` 并 `GROUP BY name HAVING name!=''`；`/api/records/latest` 的 `记录人` 同步归一（两张表显示一致）。 | 本地 + 线上实测：" 赵宏亮"(前导空格,145)→"赵宏亮"，已无带空格名字；72 人合计 8853、逐人求和=grandTotal；`node --check` 全过；GitHub Contents API 推送 + Railway 复测通过。发奖时直接用"导出Excel（全部人员）"的「总提交数」列即可。 |
| 17. Option A 计划执行补设备信息 + 导出A1修复 + 设备清单原始导出 + 标题来源标注 | 7/28 | 用户要求：① 执行 Option A（计划执行明细 JOIN base_codeinfo 补全设备信息）；② 修导出 Excel 格式 bug（消防应急灯明细表/灭火器明细列表所有数据挤在 A1 单元格）；③ 灭火器/应急灯导出设备清单时**直接导出草料原始库表** `template_codeinfo_d10`/`d15`、不做任何加工；④ 各分析标题后备注来自哪个表格。落地：① **导出 A1 根因修复**——`exportTable()` 原 `getElementById` 拿到 id 挂在 `<tbody>` 的元素，`clone.outerHTML` 只剩 `<tbody>` 无 `<table>` 包裹，Excel 无法识别结构→全塞 A1；改 `el.closest('table')` 取真正 table 根；`downloadExcelHtml` 给 `<table>` 补 `border="1"` 并新增 `escapeHtml()` 转义 `<`/`&`，防原始数据破坏表格。② **原始库表导出**：`server.js` 新增 `/api/devices/raw?type=extinguisher|light`，用 `information_schema` 取 `template_codeinfo_d10`(灭火器,533行15列)/`d15`(应急灯,83行10列) **真实列序与原值，不加工**，返回 `{table,columns,rows}`；`core.js` 新增 `exportRawDevice(type)` 从 columns+rows 拼干净 table 导出；灭火器/应急灯明细表"导出Excel"按钮改调 `exportRawDevice()`（不再导出看板加工后的台账）。③ **Option A**：`/api/tasks/details` 由 `code_task_log t LEFT JOIN base_codeinfo b ON t.code_id=b.code_id`（命中 7782/7790），`状态` 列加表别名 `t.状态` 消歧义，新增 `templates`(GROUP_CONCAT DISTINCT b.模板名称)/`categories`(b.目录) 两列；前端 `taskDetailTable` thead 加「涉及模板/目录」两列并渲染。④ **标题来源标注**：各分析区块标题后加（来源：XXX）——`base_table_data`/`table_d19`/`table_d16`/`table_d22`/`table_d27`/`code_task_log`/`template_codeinfo_d10`/`d15`/`d25` 等；动态标题（灭火器/应急灯多维分析）在 JS 补。 | 本地起服先发现并修两 bug：`devices/raw` 误用数组解构（query 直接返回 rows）+ `tasks/details` 状态列歧义（两表都有`状态`）——**未上线踩坑**；线上复测 d10=533行/d15=83行、tasks/details 7 计划带 templates/categories；前端标记 exportRawDevice/closest('table')/escapeHtml/来源标注全命中；`npm run smoke` 全绿；GitHub API 推送(6文件)+Railway 重部署完成。 |
| 18. 计划目录澄清 + 最新记录加码名称 + 多维分析布局/智能图表/列宽/趋势数值/健康提示框 | 7/28 | 用户 9 项需求：① 质疑"灭火器每月点检计划"所属目录同时显示"消防应急灯检查+灭火器检查"。**结论**：经 `code_task_log` 实测，该计划名下挂 5748 个灭火器码 + **38 个消防应急灯码** + 8 个无关联码——是草料建码时把这 38 个应急灯码归到了灭火器计划下，**库里真实数据如此，非看板逻辑错**；须到草料后台调整。② 最新提交记录（base_table_data）增「码名称」列、删「状态」列（**实测 base_table_data 本就含 `码名称` 列，无需 JOIN**）。③ 人员排行姓名列加宽到 96px 且 nowrap，保证三字姓名单行。④ 灭火/应急灯多维分析图表按分类数智能选型：≤6 类用 doughnut 饼图、>6 类用横向 bar。⑤ 灭火/应急灯多维分析**图表卡与明细表卡上下分行**（不再 grid-2 并排拥挤、字段挤成 2 行）。⑥ 全局表格列宽自适应：统一 `white-space:nowrap` + `.table-wrap` 横向滚动；急救药箱"药品明细"列特例加 `.med-cell` 允许换行。⑦ 月度记录趋势显示数值（自研 `valLabel` 插件 `afterDatasetsDraw` 描点）。⑧ 顶部多条告警统一进一个卡片框，标题"🩺 系统健康运行提醒"。 | ① `/api/tasks/details` 重写：按 `(计划名称, b.目录, b.模板名称)` 分组，Node 端按计划名累加状态计数、目录/模板取占比最高者为主目录，多类时标"含其他N类"（避免把应急灯目录并列显示误导）。② `/api/records/latest` SELECT 加 `码名称`、去 `状态`。③ 前端：renderFireAnalysis/renderLightAnalysis `useDoughnut=labels.length<=6` 动态选 chart 类型（应急灯用 12 色调色板）；灭火/应急灯 section 由 grid-2 并排拆为「图表卡(独占一行,max-width:680px居中) + 明细卡(下一行全宽)」；全局 `th,td { white-space:nowrap }` + `.med-cell{white-space:normal}`；`loadLatestRecords` 渲染码名称、`loadAlerts` 包统一健康框；`loadMonthlyTrend` 注册 `valLabel`；人员排行姓名列 `min-width:96px`。`node --check` server.js/sections.js 通过；GitHub Contents API 推送(6文件)+Railway 重部署完成。 |

---

## 三、踩过的坑与解法（价值最高部分）

### 坑 1：草料数据模型混淆（template 主数据表 vs table 表单日志）
- **现象**：急救药箱碘伏看板显示 2025-10-04（已过期 296 天），草料前台却是 2027-03-01。
- **根因**：`template_codeinfo_*` 是"子码主数据表"，存的是**码当前字段值**（草料前台展示的就是它）；`table_dXX` 只是**表单提交历史日志**，可能是建箱时一次旧提交，不代表当前状态。旧代码误读了 `table_d24`。
- **解法**：`/api/first-aid/expiry` 改读 `template_codeinfo_d25`；日期解析兼容 `YYYY/MM/DD` 与 `YYYY年MM月DD日`；修 UTC 时区导致的显示偏移一天。
- **经验**：分析"当前状态"必须认准 template 主数据表；不确定时用 `information_schema` 搜列名定位权威表。

### 坑 2：前端整段失效（一行废代码）
- **现象**：数据库状态徽章"检测中"永久转圈。
- **根因**：主 `<script>` 末尾残留一行 `}  }).join('');` 废代码，导致**整个脚本解析失败、完全不执行**，`loadHealth()` 从未被调用。
- **解法**：抽取 `<script>` 块用 `node --check` 定位到具体行，删除后复测通过。
- **经验**：大文件多处 Edit 后，务必用 `node --check` 复核前端脚本语法，避免"整段不执行"类隐性故障。

### 坑 3：沙箱网络拦截 git 推送
- **现象**：`git push`/`git fetch` 到 github.com:443 偶发 `Connection was reset` 或连接超时；`git ls-remote` 有时却通，极具迷惑性。
- **解法**：改用 **GitHub REST Contents API**（小 HTTPS 请求，`api.github.com` 在沙箱内可达）：`GET /contents/{path}` 拿 sha → `PUT` 同路径（body 含 base64 内容 + sha）逐文件提交。两次提交均 HTTP 200，Railway 自动重部署成功。
- **经验**：沙箱里 git 大传输被拦时，优先用 GitHub API 推小文件；大仓/多文件仍需解决网络出口。

### 坑 4：GitHub push 误判成功
- **现象**：推送重试循环靠 `grep "main -> main"` 判断成功，实际没推上去，GitHub HEAD 停在旧 commit。
- **解法**：用 `gh api repos/.../commits/main --jq '.sha'` 校验远端 HEAD 是否等于目标 commit SHA。后续统一以"远端 SHA 比对"为唯一判据。

### 坑 5：时区偏移
- **现象**：日期显示比实际差一天。
- **根因**：UTC 解析致偏移。
- **解法**：用本地年月日拼 ISO，避免 UTC 转换。

### 坑 6：`query()` 返回的是行数组，不能直接解构取"整表"
- **现象**：应急灯多维度接口首版返回 `total=0`、`byDevice` 只是首行对象而非数组，图表维度数全错。
- **根因**：本项目的 `query()` 封装直接 `return rows`（**数组**），而首版代码写成 `const [byDevice] = await query(...)`，把"第一行"当成了"整张结果集"。
- **解法**：聚合类查询用 `const byDevice = await query(...)`（拿到完整数组），仅 `SELECT COUNT(*)` 这类单行结果才用 `const [row] = await query(...)`。
- **经验**：封装返回数组的查询函数，对"期望多行"的查询**不要**用数组解构；不确定返回结构时先用 `node -e` 直连打印验证再写前端。

### 坑 7：应急灯"设备分析"应认准主数据表 d15，而非点检日志 d16
- **现象**：阶段 9~12 的应急灯多维度分析一直从 `table_d16`（点检提交历史，1149 条记录）聚合，用户指出"应急灯只有 83 台，分析应从 `template_codeinfo_d15`（子码主数据）取"。
- **根因**：把"点检记录数"当成了"设备数"——`table_d16` 是每次点检一条的日志，一台灯被点检多次会重复计数；设备本身的属性（责任部门/厂商/安装位置/点检人/状态）都在 `template_codeinfo_d15` 主数据表里，且 d15 无 `码名称` 列（用 `code_id` 关联）。
- **解法**：`/api/emergency-lights/analysis` 重写为纯 d15 聚合（`total=83`），维度取自 `点检人_3170560`/`责任部门_81036793872385`/`生产厂家_3170558`；明细改为 d15 设备台账，移除"点检异常明细"。d15 无时间字段，故分析不套用时间切片。
- **经验**：做"设备/资产维度"分析时，数据源必须是**主数据表**（当前状态），不要拿**表单日志表**（行为流水）当设备清单；一台设备多条日志会严重夸大分母。

---

## 四、做得好的地方

- **架构选择正确**：坚持"直连官方库实时查询"，天然实时、无同步滞后（详见技术文档与用户问答）。
- **每次修复都线上复测**：所有 bug 修复后都在 Railway 实测 API 返回 200、数据正确，不靠"本地能跑"臆断。
- **把隐性需求当硬指标**：用户提的"数据要跟草料一致""超期只显示最新周期"都作为验收标准落实并验证。
- **前端健壮性演进**：从会永久卡死的 `Promise.all` 演进到 `allSettled + 超时 + finally` 兜底 + no-cache，可用性显著提升。

---

## 五、待改进（详见《优缺分析与提升计划》）

> 阶段 7 已完成 P0 四项加固（缓存 / 鉴权 / 容错 / 前端组件化），原"待改进 3/4/5"已闭环。剩余：

1. 部署链路对 GitHub 强依赖（且本环境推送受网络限制），可评估不依赖 GitHub 的部署方式。
2. 缺少自动化测试与 CI，回归靠人工线上验证（建议 P1 补冒烟脚本 + 推送即跑）。
3. ~~无缓存层~~ → **已实施**：统计接口默认缓存 60s（`CACHE_TTL` 可调，0=关闭），附 `/api/cache/stats`、`/api/cache/clear`。
4. ~~前端单文件巨石~~ → **已实施**：拆分为 `public/js/core.js`（全局/工具/插件/令牌）、`sections.js`（各模块 loader）、`app.js`（调度/初始化），任一处语法错误不再拖垮整页。
5. ~~无访问控制~~ → **已实施**：令牌鉴权（默认 `cli-dash-2026`，生产请改 `ACCESS_TOKEN` 环境变量；可选 `ALLOWED_IPS` 白名单），健康检查 `/api/health` 免鉴权便于监控。

---

| 19. 安全加固：令牌改 Header 传输 + 零依赖限流 | 7/29 | 用户就 other-agent 提出的 8 条改进建议征询取舍。评估后仅采纳 #2（令牌改 Header）与 #5（限流），其余（多用户角色/分页/CORS收紧/WebSocket/移动端）对单组织只读内部看板属过度设计；#6 缓存经核已存在。落地：① **前端令牌改 Header**：`public/js/core.js` 的 `validateToken()` 与 `fetchAPI()` 不再把令牌拼在 URL（`?token=`），改为请求头 `x-api-token`（服务端 `requireAuth` 早已支持，无需改后端）；`initAuth` 保留一次性 `?token=` 深链引导（读后立即 `history.replaceState` 从地址栏抹除）。真实暴露面（服务器访问日志/浏览器历史）消除。② **零依赖限流**：`server.js` 新增内存限流器 `rateLimiter`（`app.use('/api', rateLimiter)` 置于鉴权前），按 `x-forwarded-for`/remoteAddress 分桶固定窗口，`RATE_LIMIT=120`/`RATE_WINDOW=60` 默认，超阈值返回 429。③ `docs/技术文档.md` §4/§7 同步（访问控制改为 Header 鉴权+限流、缓存已存在），并将其加入 `deploy-via-api.js` 推送清单（原只推 6 运行文件）。 | 线上验证：Header 令牌=200 / URL 令牌=200(向后兼容) / 无令牌=401；本地稳定 IP 突发 126 次=120×200+6×429 证实限流逻辑正确；线上突发 0×429 系沙箱出口 IP 漂移致分桶（真实稳定客户端会触发）。GitHub Contents API 推送 7 文件 + Railway 重部署完成。 |

| 20. 时间切片改日历对齐范围（北京时间） | 7/29 | 用户要求：顶部快捷范围删除"近30天/近90天/近1年"，增加"本周/本月/上月/本年"，均按北京时间算（本周=周一0点起、本月=1日起、上月=整月、本年=元旦起）。落地：① `public/index.html` 快捷按钮改为 `setQuickDate('week'|'month'|'lastMonth'|'year')`，保留`近7天`(数字7)与`全部`('all')。② `public/js/core.js` 重写 `setQuickDate`：用 `new Date(n.getTime()+n.getTimezoneOffset()*60000+8*3600000)` 取**北京时间 instant**，再经 `getUTC*` 取 Y/M/D 分量（**不依赖浏览器所在时区**）；新增 `ymd()` 拼串。周一偏移 `(dow+6)%7`；上月跨年用 `Date.UTC(ly,lmm+1,0)` 取末日。③ 服务端切片条件依赖 `TZ='Asia/Shanghai'`（阶段10 已设），前端给北京日期串与后端比较口径一致。 | Node 模拟 2026-07-29(周三)→本周 07-27~07-29/本月 07-01~07-29/上月 06-01~06-30/本年 01-01~07-29；跨年基准 2026-01-15→上月 2025-12-01~12-31 均正确；`node --check` core.js 通过；GitHub Contents API 推送 + Railway 重部署后线上复测 index.html 新按钮 + core.js 新分支均命中。 |

| 21. 修复时间切片"少算一天"边界 bug | 7/29 | 用户新增 2 条今日(北京07-29 06:08/06:09)记录后，仍必须手改截止日为 07-29 才能看到——暴露前端边界 bug。根因：`setQuickDate` 旧公式 `new Date(n.getTime()+n.getTimezoneOffset()*60000+8*3600000)` 在浏览器 UTC+8 时 `getTimezoneOffset()=-480` 把 +8h 抵消，随后 `bj.getUTCDate()` 取到 UTC 分量（北京 07-29 06:15 对应 UTC 07-28 22:15 → 返回 28），所有切片 endDate 少一天；Node 测试在 UTC 时区(offset=0)碰巧通过致漏网。修复：去掉 `getTimezoneOffset()` 补偿，改为绝对 instant+8h（`getUTC*` 分量恒为北京日历，与浏览器时区无关）；注释同步说明陷阱。新增 `scripts/verify-quickdate.js` 在 `TZ=Asia/Shanghai`(offset=-480) 复现 buggy(week/month/year/all 的 end 全=07-28) 并验证 fixed 全=07-29。 | 上海时区脚本复现+验证通过；线上复测 `core.js` 旧公式残留 0 处；后端 `endVal` 逻辑本就正确——`startDate=2026-07-27&endDate=2026-07-29` 已返回用户今日 2 条新记录。GitHub Contents API 推送 + Railway 重部署完成；提醒用户硬刷新避缓存。**教训**：前端"北京时间"计算必须在 UTC+8 而非 UTC 时区测，避免 `getTimezoneOffset()` 抵消类写法。 |

| 22. 日期框统一中文「年/月/日」显示 | 7/29 | 用户反馈原生 `<input type="date">` 显示受浏览器/系统区域控制，出现 "yyyy/mm/日" 中英文混合样式，要求统一为「年/月/日」。落地：改用 flatpickr（`cdn.jsdelivr.net/npm/flatpickr` + `dist/l10n/zh.js` + css），`altInput:true` + `altFormat:'Y年m月d日'`，可见框显示中文、隐藏原 input 仍存 ISO `YYYY-MM-DD`，故 `sections.js` 全部 `getElementById('startDate').value` 取值逻辑零改动；`setQuickDate` 改用 `fpStart/fpEnd.setDate(s,true)` 并保留 CDN 失效时的原生回退；输入框占位符置「年 / 月 / 日」；CSS 选择器由 `input[type=date]` 扩为含 `input[type=text]`。 | `node --check` core.js 通过；CDN 三资源均 200 可达；GitHub Contents API 推送 + Railway 重部署后线上复测 index.html 含 flatpickr 引入、`core.js` 含 `flatpickr.localize`/`fpStart = flatpickr`/`fpStart.setDate`/`altFormat:'Y年m月d日'`。 |

| 23. 安全/性能加固（采纳 review-report 🔴批次） | 7/29 | 用户就另一 agent 的评测报告（P0 密钥泄露/16处XSS/500透传/CORS*/无安全头/性能 等）决定**只做🔴代码侧批次**，P0 密钥轮换需线下配合故延后。落地：① **S1 渲染层 XSS 转义**：`core.js` 的 `escapeHtml()` 扩展转义 `"`/`'`（原仅 `& < >`），`sections.js` 全部 16 处 `innerHTML` 拼接的数据库动态字段（人员姓名/记录单名称/记录人/码名称/设备属性/下拉 option 的 value 与 text 等）统一包 `escapeHtml`；`app.js` 数据库状态/错误文本也转义（health 已脱敏，残留无泄露但补牢）。② **S2 500 错误脱敏**：新增 `sendError(res,err)`，生产环境返回通用文案 `服务器内部错误，请稍后重试或联系管理员。`（不暴露 SQL/表结构），服务端 `console.error` 记完整堆栈，仅 `NODE_ENV==='development'` 回显原文；所有路由 `catch` 改 `sendError(res,err)`。③ **S3 移除 CORS 通配**：删 `app.use(cors())` 与 `require('cors')`（同源部署）。④ **S4 安全响应头**：`app.disable('x-powered-by')` + 加 `X-Content-Type-Options: nosniff` / `X-Frame-Options: DENY` / `Referrer-Policy: no-referrer`；**未强制 CSP**（页面含大量内联脚本与 Chart.js 插件，严格 CSP 会破坏渲染，已向用户说明）。⑤ **S5 health 脱敏**：`/api/health` 仅返回 `{status:'ok'}`，去掉 `数据库连接正常` 详情。⑥ **性能**：`/api/stats` 的 5 个独立 COUNT/聚合改 `Promise.all` 并行（首查约 2.5s→1s 内）；引入 `compression` 做 gzip（首页 28KB/JS 45KB 均压缩，线上 `Content-Encoding: gzip` 已验证）；缓存 Map 加 5 分钟清理过期项 + 500 上限防内存泄漏；移除 mysql2 无效配置 `timeout:10000`/`acquireTimeout`（仅留 `connectTimeout`）；启动预热 `warmUp()` 填充核心统计缓存（本地验证连通 RDS）。⑦ **S6**：`public/index.html.bak` 移出 public 静态目录（移到项目根，避免被静态服务暴露）。 | 线上验证（curl）：`/api/health`→`{"status":"ok"}`、响应头含 `x-content-type-options`/`x-frame-options`/`referrer-policy`、无 `X-Powered-By`、无 `Access-Control-Allow-Origin`、首页 `Content-Encoding: gzip` 全绿；`node --check` + 本地启动 `warmUp` 连通 RDS 通过。⚠️ **关键部署坑**：deploy 脚本原 `FILES` 只推 7 个运行文件（不含 package.json），新增 `compression` 依赖后必须把 `package.json`/`package-lock.json` 纳入推送清单，否则 Railway 按旧 package.json 安装会致 `require('compression')` 崩溃——已修正并推送 9 文件。⚠️ **P0 未做（待用户线下）**：GitHub OAuth token 硬编码于 `scripts/deploy-via-api.js`、数据库密码 fallback 于 `server.js`、默认访问令牌 `cli-dash-2026` 仍生效——三项属"后门钥匙"，须按 review 报告吊销/轮换/设环境变量 fail-closed 方可真正闭环。 |
| 24. 设备分布三修（归类判序 bug / 其他7 / 覆盖天数） | 7/30 | 用户三问：① d10 灭火器 533，卡片为何 600？② 设备卡片改单行 + "其他7"是什么？③ 7/1~7/30 覆盖天数为何 29？逐一核实修复：① **灭火器 600=533+67 是 `deviceKey()` 判序 bug**——目录"气体灭火器系统储气瓶"（67 码）同时含"灭火器/气瓶"，归类函数先判"灭火器"致 67 个储气瓶误并入；修复为先判"气瓶"（并删原靠后的重复判断），灭火器回归 533、与 d10 完全一致。② **"其他7" 查明** = 环保公示类：奥科宁克环保排放口二维码 6（生活污水/雨水/熔铸废气/热轧废气/冷轧废气/生产废水排放口）+ 土壤污染重点监管单位公示信息 1（目录=全部活码）；deviceKey 新增 `环保|排放→环保公示` 类，卡片现示 环保公示6 + 其他1。设备卡片改单行 `.stats-row-inline`（flex nowrap + 横向滚动）。③ **覆盖天数差一天**：前端原 `Math.ceil(diff/86400000)`（7/1~7/30 相减=29），改 `Math.floor(...)+1` 含首尾 → 30；同日=1。诊断手段：`/api/code-by-device` 新增 `?detail=1`（目录×模板×状态细分）——本地已无 DB_PASSWORD，数据诊断一律走线上 API。 | 线上验证：`/api/code-by-device` → 灭火器533/应急灯83/气瓶67/压力表59/洗眼器12/急救药箱10/环保公示6/应急物资3/其他1/喷淋泵1，合计仍 775；线上 sections.js 含 `+ 1` 修复、index.html 含 `stats-row-inline`。GitHub push 自动部署（约 50s 生效）。 |
| 25. 看板四项体验优化（汇总行/图表裁切/药品对称/参与人数口径） | 7/30 | 用户四项：① 计划执行明细表底部要汇总；② 灭火器强制报废预警选"1年内"只有 2 根柱，柱顶数字"1"被上边缘裁切；③ 急救药箱 8 种药品显示成 5+3 不对称；④ 顶部"参与人数 77"不随时间切片、与 TOP20 对不上。落地：① `index.html` 表内加 `<tfoot id="taskDetailFoot">` + 全局 `tfoot td` 样式（浅灰底/加粗/sticky bottom），`loadTaskDetails()` 末尾累加总数/完成/超期完成/超期未完成/未完成并算总完成率（口径同行内：仅 `完成`，不含超期完成）；`exportTable()` 用 `closest('table')` 取根，tfoot 自动进 Excel。② 新增模块级 `barValueLabelPlugin`（Chart.js 内联插件，`afterDatasetsDraw` 在柱顶绘数值，不引入 CDN），配套 `layout.padding.top=24` + `suggestedMax = max + max(1, ceil(max*0.15))` + `ticks.precision=0`（max≤5 时 `stepSize=1`，杜绝 0.2/0.4 小数刻度）+ 容器 `chart-h200→chart-h250`。③ `.med-wrap` 由 `flex-wrap` 改 `grid + repeat(4, minmax(78px,1fr))`，8 种药品固定 4+4 两行对称，窄屏（≤900px）降 2 列。④ `/api/stats` 的 `memberCount` 由 `COUNT(*) FROM base_auth_msg`（77，全部授权账号、恒定不随切片）改为 `COUNT(DISTINCT norm(记录人)) FROM base_table_data + dateWhere`，与 `/api/members/ranking` 完全同源同口径。 | 线上验证：全量 `/api/stats` memberCount=**72** 与 `/api/members/ranking` 的 `members.length`=72 一致；切片 7/1~7/30 两边同为 **51**，联动成立（77→72 的差额 5 人 = 有账号但从未提交记录者）。前端资源校验：sections.js（45724B）含 `taskDetailFoot`/`barValueLabelPlugin`/合计行文案，index.html（32646B）含 `<tfoot>`、`repeat(4, minmax(78px`、`chart-h250`、`tfoot td` 样式。GitHub push 自动部署生效。 |

### 阶段 27：图表渲染统一整改（2026-07-30）
| 问题 | 根因 | 修复 |
|---|---|---|
| 强制报废图柱顶出现两个重叠数字 | `core.js` 已全局 `Chart.register(barLabelPlugin)`，阶段26 又给该图挂了内联 `barValueLabelPlugin`，两插件同位置各画一遍（11px/-3px vs 12px/-4px） | 删除内联插件，统一由全局插件绘制；并在 core.js 插件上方加禁止重复挂载的警示注释 |
| 维修充装图 Y 轴出现 0.2/0.4 小数刻度、最高柱数字被上边缘裁切 | 该图仍是最初的 `scales:{y:{beginAtZero:true}}`，未做整数化与顶部留白 | 抽出 `countAxis(values)` 共用助手（整数刻度 + suggestedMax 留白 + 隐藏网格线），配 `padding.top:26`，容器 h200→h250 |
| 各分析图 Y 轴网格线冗余 | 早期写死 `grid:{color:'#f1f5f9'}` | `countAxis` / `hBarScales` 统一 `grid:{display:false}` |
| 切到"按责任人/责任部门/生产厂家"时 Y 轴文字显示不全 | 横向条形图容器高度固定 300px，分类多时行高被压缩 | 新增 `fitChartHeight(canvasId,count,useDoughnut)`，按分类数撑高容器至 `max(300, count*30+70)`px；`y.ticks.autoSkip:false` 保证标签不被跳过 |
| 横向条形图数值标签位置错位 | 全局插件按纵向柱假设绘制 | 插件识别 `indexAxis==='y'`，改画在柱右侧（left/middle），配 `padding.right:34` |

覆盖图表：灭火器维修充装预警、灭火器强制报废预警、灭火器/消防应急灯/压力表/气体灭火器储气瓶 4 个多维度分析。

### 阶段 28：计划执行明细"未完成"列漏算"即将超期"状态（2026-08-10）
| 问题 | 根因 | 修复 |
|---|---|---|
| 选"本月"时灭火器计划显示 total=533/完成457/未完成0，数据对不上（应为未完成76）；应急物资 total=3/完成0/未完成0 同理 | 草料周期任务状态枚举有 5 种：完成/未完成/超期完成/超期未完成/**即将超期**（及 NULL/空）。`/api/tasks/details` 的 `SUM(CASE WHEN 状态=...)` 只覆盖前 4 种，漏了"即将超期"（=任务已建但临近截止、尚未点检）。该状态记录被 `COUNT(*)` 计入 `total` 却没被任何 CASE 捕获 → incomplete=0。应急灯/洗眼器等的未完成是真正的 `未完成` 字符串故正常显示，反衬出这是枚举遗漏而非 CASE 写错 | 在 `incomplete` 的 CASE 中把 `即将超期` 以及 `NULL`/空值一并计入（业务语义：未做=未完成）；`overdue_incomplete` 仍仅含 `超期未完成`。临时加 `/api/debug/task-raw-status` 查原始状态枚举确认后已删除 |

线上验证：本月灭火器 total=533/完成457/**未完成76** ✓；应急物资 total=3/完成0/**未完成3** ✓；其余计划 incomplete（应急灯15/洗眼器11/急救药箱4/喷淋泵1）不变；合计完成率=545/655≈83.2%。

### 阶段 29：计划执行明细下方新增"未完成任务设备清单"查看/导出按钮（2026-08-10）
- 需求：在计划执行明细表下方加按钮，列出所有"非已完成"设备（编号/位置/责任部门/点检人/任务结束日期等），导出 Excel 后可直接发邮件提醒。
- 新增后端 `/api/tasks/pending-devices`：取 `code_task_log` 中 `状态 != '完成'`（含 未完成/超期未完成/超期完成/即将超期/NULL/空）的记录，按 `开始时间` 与计划执行明细同源切片；通过 `code_id` 把 6 张设备主数据模板表（d10/d12/d14/d15/d25/d28，均含 `code_id` 列）全部 `LEFT JOIN` 后用 `COALESCE` 兜底取 编号/位置/责任部门/点检人；任务结束日期=`截止时间`。
- 前端：`index.html` 计划执行明细卡片下新增"🔍 查看/导出未完成任务设备"按钮 + 结果卡片；`sections.js` 新增 `showPendingDevices/exportPendingDevices/buildPendingTableHtml`，复用 `core.js` 的 `downloadExcelHtml` 导出。
- 数据限制（非代码缺陷）：洗眼器/应急物资/喷淋泵/部分急救药箱这 19 条在草料中**无对应设备主数据模板表**（6 张模板表都不含其 code_id），编号回退为码名称、位置/责任部门/点检人显示"-"。灭火器(76)/应急灯(15) 字段完整。
- 线上验证：本月共 **110** 条未完成任务，按类型分布与计划执行明细完全一致（灭火器76/应急灯15/洗眼器11/应急物资3/急救药箱4/喷淋泵1）；灭火器样本字段齐全（编号KF30/位置薄剪工段/责任部门生产部精整车间薄剪段/点检人班长/结束日期2026-08-10）。

## 六、一句话总结

本项目用"**直连草料官方只读库 + 轻量 Express/Chart.js 看板 + Railway 云端部署**"的组合，把一个原本分散、滞后的安全设备数据，变成了团队随时可看、与平台实时同步的分析中心；过程中最大的认知收益是**分清草料的主数据表与表单日志**，最大的工程收益是**前端容错 + 部署可观测**。

### 阶段 30：CI 冒烟测试（Smoke Test）恒失败 → Gmail 收大量失败邮件（2026-08-10）
| 问题 | 根因 | 修复 |
|---|---|---|
| Gmail 持续收到 `[xiaohuoshuan123/cli-dashboard] Run failed: Smoke Test - main` 失败通知，且一次部署炸出十几封 | ① 矛盾点：`smoke.yml` 的冒烟测试明确设计为"不依赖数据库、CI 必定可跑"，但 `server.js` 尾部启动校验是 fail-closed——缺 `DB_PASSWORD` 就 `process.exit(1)`；CI 不注入 `DB_PASSWORD` → 服务起不来 → 冒烟超时失败 → GitHub 发邮件。② 邮件成倍：`scripts/deploy-via-api.js` 用 GitHub Contents API **逐文件 PUT**，15 个文件 = 15 次独立 commit/push = 15 次 CI 运行，一次部署即十几封失败邮件 | `server.js` 启动校验改为**仅生产环境强制**（读 `NODE_ENV==='production'`）：生产缺 `DB_PASSWORD`/`ACCESS_TOKEN` 仍 `exit(1)`（fail-closed 不变）；非生产（CI 冒烟 `NODE_ENV=test`、本地调试）缺 `DB_PASSWORD` 时以"降级模式"启动（仅无 DB 接口可用），`warmUp/keepPoolAlive` 的 DB 失败已被 try/catch 吞掉不拖垮进程。本地 `npm run smoke` 验证 `EXIT=0` 全 ✓。部署脚本的"15 文件=15 提交"问题单列待优化（建议改用单次提交以省 CI 额度） |

线上验证：本地 `npm run smoke` 语法检查 4 文件全 ✓ + 鉴权校验全 ✓（/api/auth/verify 正确→200、错误→401、无令牌→401、/api/health→200），`EXIT=0`。
